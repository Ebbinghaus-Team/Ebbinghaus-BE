spring:
  application:
    name: ttopullae


  datasource:
    url: jdbc:mysql://localhost:3306/main_db
    username: root
    password: asd1234
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true

# JWT 설정
jwt:
  secret-key: ebbinghaus-ttopullae-secret-key-for-jwt-token-generation-and-validation-2025
  token-expiration-seconds: 86400  # 1일 (24시간)
  cookie-expiration-seconds: 86400  # 1일 (24시간)

springdoc:
  default-consumes-media-type: application/json;charset=UTF-8
  default-produces-media-type: application/json;charset=UTF-8
  swagger-ui.display-request-duration: true
  swagger-ui:
    path: /docs/swagger
